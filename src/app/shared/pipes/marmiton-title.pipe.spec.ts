import { MarmitonTitlePipe } from './marmiton-title.pipe';

describe('MarmitonTitlePipe', () => {
  it('create an instance', () => {
    const pipe = new MarmitonTitlePipe();
    expect(pipe).toBeTruthy();
  });
});
